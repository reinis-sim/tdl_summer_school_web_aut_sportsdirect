import HomePage from "../pageObjects/HomePage";

context("SportsDirect", () => {
    context("Checkbox scenarios", () => {
      beforeEach(() => {
        HomePage.visit();
      });

      it("Progress bar scenario 1", () => {
        HomePage.brands.click();

      });










    });
});